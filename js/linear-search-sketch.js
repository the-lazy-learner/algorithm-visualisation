let values = [];

function setup() {
  for (let i = 1; i <= 10; i++) {
    values.push(Math.floor(random()*101))
  }
  // createCanvas(640,480);
  // let h1 = document.createElement('h1');
  // h1.innerText = values.join(', ');
  // document.body.querySelector('main').appendChild(h1);
}

function draw() {
}